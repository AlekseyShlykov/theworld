# Optional: Google Apps Script Web App URL for analytics.
# If set, choice events are sent to this endpoint (POST).
# If unset or empty, analytics are not sent.
# VITE_ANALYTICS_WEB_APP_URL=https://script.google.com/macros/s/.../exec
